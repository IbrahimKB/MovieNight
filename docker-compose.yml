version: "3.8"
services:
  movienight:
    build: .
    ports:
      - "80:3000"            # Host port 80 → container 3000
    restart: unless-stopped
    environment:
      NODE_ENV: production
      # Add any env-vars your backend needs:
      # - DATABASE_URL
      # - API_KEYS, etc.
    volumes:
      # If you have uploads or persistent data:
      - ./data:/app/data
